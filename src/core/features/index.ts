import { busFeature } from '@features/bus';
import { greetingFeature } from '@features/greeting';
import { versionFeature } from '@features/version';

export const features = [greetingFeature, busFeature, versionFeature];
